# This is a reproduced and slightly curated list of populated places.

this sf/tibble/data.frame data set is derived from the U.S. Census
Bureaus tigris::places() data set. It has only the centroid of each
place rounded to three decimal places to reduce size.

## Usage

``` r
data(google_towns)
```

## Format

Each is a tibble with two variables and three observations

---
output: pdf_document
geometry: paperheight=4in,paperwidth=4in,margin=0.1in
header-includes:
  - \pagenumbering{gobble}
---
```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
knitr::opts_chunk$set(dpi = 300) 
knitr::opts_chunk$set(message = FALSE)
knitr::opts_chunk$set(warning = FALSE)
```

```{r load library}
library(BarnebyLives)
```

```{r load data}
data <- collection_examples
writer <- function(x){if (is.na(x)){""} else {x}}
writer_ital <- function(x){if (is.na(x)){""} else {paste0('*', x, '*')}}
```

\fontsize{14pt}{14pt}
\begin{center}
\textbf{Seeds of Success}
\end{center}
\fontsize{12pt}{12pt}
`r data$Family`  
*`r paste(data$Genus, data$Epithet)`* `r data$Binomial_authority` `r writer(data$Infrarank)` `r writer_ital(data$Infraspecies)` `r data$Infraspecific_author` 

\vspace*{\fill}
\fontsize{10pt}{10pt}
`r data$latitude_dd` `r data$longitude_dd`. `r data$Gen`. `r writer(data$Site)` 

`r data$physical_environ` `r data$Habitat`.  
Veg.: `r species_font(data$Vegetation)`. Ass.: `r species_font(data$Associates)`

\vspace*{\fill}
\fontsize{12pt}{12pt}

`r data$Primary_Collector` `r data$Collection_number`,  `r writer(data$Associated_Collectors)`  
`r data$Date_digital_text` 

`r writer_fide(data$Fide)`
